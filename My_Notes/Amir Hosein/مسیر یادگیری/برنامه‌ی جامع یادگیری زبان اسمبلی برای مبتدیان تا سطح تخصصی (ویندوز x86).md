
---

## مقدمه

زبان اسمبلی یکی از بنیادی‌ترین و در عین حال چالش‌برانگیزترین زبان‌های برنامه‌نویسی است که به برنامه‌نویسان امکان تعامل مستقیم با سخت‌افزار و سیستم‌عامل را می‌دهد. با وجود ظهور زبان‌های سطح بالا، اسمبلی همچنان در حوزه‌هایی مانند برنامه‌نویسی سیستم، بهینه‌سازی عملکرد، مهندسی معکوس، امنیت، توسعه بوت‌لودرها و درایورها، و حتی درک عمیق‌تر از عملکرد کامپیوترها جایگاه ویژه‌ای دارد. هدف این برنامه، ارائه‌ی یک مسیر یادگیری مرحله‌ای و ساختارمند است که شما را از سطح مبتدی به سطح متخصص در زبان اسمبلی، به‌ویژه روی پلتفرم x86 و سیستم‌عامل ویندوز، هدایت می‌کند.

در این گزارش، ابتدا مفاهیم تئوری و پایه‌ای زبان اسمبلی را به‌صورت کامل بررسی می‌کنیم، سپس به آموزش عملی نوشتن، اسمبل و اجرای کدهای اسمبلی روی ویندوز x86 می‌پردازیم. ابزارهای مورد نیاز، نحوه نصب و راه‌اندازی، دیباگرها، شبیه‌سازها و محیط‌های توسعه مناسب معرفی و مقایسه می‌شوند. همچنین تمرین‌های روزانه، پروژه‌های کوچک و منابع یادگیری برای خودآموزی و پیشرفت مستمر ارائه خواهد شد. در نهایت، مفاهیم پیشرفته برنامه‌نویسی سیستم، مدیریت حافظه، وقفه‌ها، رجیسترها، تعامل با سخت‌افزار و امنیت پوشش داده می‌شوند.

---

## ۱. مفاهیم تئوری زبان اسمبلی: از صفر تا پیشرفته

### ۱.۱. تعریف و جایگاه زبان اسمبلی

زبان اسمبلی (Assembly Language) یک زبان سطح پایین است که هر دستور آن تقریباً معادل یک دستور ماشین خاص همان معماری پردازنده است. برخلاف زبان‌های سطح بالا که مستقل از معماری هستند، هر اسمبلی مختص یک معماری (مثلاً x86 یا ARM) است. این زبان به برنامه‌نویس اجازه می‌دهد تا کنترل دقیقی بر منابع سخت‌افزاری، حافظه و رجیسترها داشته باشد.

**کاربردهای اصلی زبان اسمبلی:**

- توسعه سیستم‌عامل‌ها، بوت‌لودرها و درایورها
- برنامه‌نویسی سیستم و تعامل مستقیم با سخت‌افزار
- بهینه‌سازی شدید عملکرد (Performance Tuning)
- مهندسی معکوس و تحلیل باینری
- امنیت و توسعه ابزارهای امنیتی (مانند کرک و تروجان)
- آموزش معماری کامپیوتر و درک عمیق‌تر از عملکرد سیستم‌ها

### ۱.۲. معماری x86 و تفاوت‌های ۱۶/۳۲/۶۴ بیتی

معماری x86 ابتدا با پردازنده 8086 (۱۶ بیتی) معرفی شد و سپس به ۳۲ بیت (x86 یا IA-32) و ۶۴ بیت (x86-64 یا x64) توسعه یافت. هر نسل، رجیسترها و قابلیت‌های بیشتری را ارائه می‌دهد:

- **۱۶ بیت (8086/80286):** رجیسترهای AX, BX, CX, DX و رجیسترهای سگمنت (CS, DS, ES, SS)
- **۳۲ بیت (80386 به بعد):** رجیسترهای EAX, EBX, ECX, EDX, ESI, EDI, ESP, EBP
- **۶۴ بیت (x86-64):** رجیسترهای RAX, RBX, RCX, RDX, RSI, RDI, RSP, RBP و R8 تا R15

در معماری ۳۲ بیت، حداکثر ۴ گیگابایت حافظه قابل آدرس‌دهی است، در حالی که در ۶۴ بیت این مقدار به صورت نظری تا ۱۶ اگزابایت افزایش می‌یابد.

### ۱.۳. اجزای اصلی معماری x86

**رجیسترها:**

- **عمومی (General Purpose):** EAX, EBX, ECX, EDX, ESI, EDI, ESP, EBP
- **اشاره‌گرها:** ESP (Stack Pointer)، EBP (Base Pointer)
- **سگمنت‌ها:** CS, DS, ES, SS, FS, GS
- **پرچم‌ها (Flags):** EFLAGS (شامل Zero, Sign, Carry, Overflow و غیره)
- **Instruction Pointer:** EIP (در ۳۲ بیت)، RIP (در ۶۴ بیت)

**حافظه:**

- **Stack:** ساختار LIFO برای ذخیره موقت داده‌ها و آدرس بازگشت توابع
- **Heap:** حافظه پویا که توسط برنامه در زمان اجرا تخصیص می‌یابد
- **Data, BSS, Text Sections:** بخش‌های مختلف برای داده‌های مقداردهی‌شده، مقداردهی‌نشده و کد اجرایی.

### ۱.۴. مفاهیم کلیدی اسمبلی

- **اپ‌کد (Opcode):** کد عملیاتی که نوع دستور را مشخص می‌کند (مثلاً MOV, ADD, SUB)
- **عملوند (Operand):** داده یا آدرسی که دستور روی آن عمل می‌کند
- **آدرس‌دهی (Addressing):** روش‌های مختلف برای اشاره به داده‌ها (Immediate, Direct, Indirect, Indexed)
- **برچسب (Label):** نام نمادین برای آدرس‌ها، معمولاً برای پرش‌ها و حلقه‌ها
- **ماکرو (Macro):** بلوک کد قابل استفاده مجدد
- **بخش‌ها (Sections):** .text (کد)، .data (داده مقداردهی‌شده)، .bss (داده مقداردهی‌نشده)

### ۱.۵. دستورات پایه و ساختار کد اسمبلی

**دستورات متداول:**

- **MOV:** انتقال داده بین رجیسترها یا حافظه
- **ADD/SUB/MUL/DIV:** عملیات ریاضی
- **AND/OR/XOR/NOT:** عملیات منطقی
- **PUSH/POP:** کار با استک
- **JMP/JE/JNE/JG/JL:** پرش‌ها و شاخه‌بندی
- **CALL/RET:** فراخوانی و بازگشت از توابع
- **CMP:** مقایسه دو مقدار و تنظیم پرچم‌ها
- **INT:** فراخوانی وقفه نرم‌افزاری (مثلاً INT 0x80 در لینوکس)

**ساختار کلی برنامه:**

```assembly
section .data
    msg db 'Hello, World!', 0

section .text
    global _start
_start:
    ; کد اصلی برنامه
    mov eax, 4
    mov ebx, 1
    mov ecx, msg
    mov edx, 13
    int 0x80
    mov eax, 1
    xor ebx, ebx
    int 0x80
```

این مثال برای لینوکس است؛ در ویندوز، باید از APIهای ویندوز استفاده کرد.

### ۱.۶. سینتکس‌ها و دیالکت‌های اسمبلی (Intel vs AT&T، NASM، MASM، FASM)

**سینتکس Intel:** رایج‌ترین سینتکس در ویندوز و ابزارهایی مانند NASM و MASM. ترتیب عملوندها: مقصد، مبدا (destination, source). رجیسترها بدون پیشوند، مقادیر فوری مستقیم، آدرس‌دهی با براکت.

**سینتکس AT&T:** بیشتر در لینوکس و ابزارهایی مانند GAS و GCC. ترتیب عملوندها: مبدا، مقصد (source, destination). رجیسترها با پیشوند %، مقادیر فوری با $، آدرس‌دهی با پرانتز.

|ویژگی|Intel Syntax|AT&T Syntax|
|---|---|---|
|ترتیب عملوندها|dest, src|src, dest|
|رجیستر|eax|%eax|
|مقدار فوری|10|$10|
|آدرس‌دهی حافظه|[ebx+4]|4(%ebx)|
|کامنت|;|//|

**تحلیل:** برای یادگیری و توسعه روی ویندوز، سینتکس Intel پیشنهاد می‌شود، زیرا همخوانی بیشتری با منابع آموزشی، دیباگرها و ابزارهای ویندوز دارد.

**اسمبلرهای رایج:**

- **NASM:** رایگان، متن‌باز، قابل استفاده در ویندوز و لینوکس، سینتکس Intel
- **MASM:** محصول مایکروسافت، سینتکس Intel، یکپارچه با Visual Studio
- **FASM:** سریع، متن‌باز، قابلیت تولید مستقیم فایل اجرایی
- **GAS:** بیشتر در لینوکس و یونیکس، سینتکس AT&T

---

## ۲. آموزش عملی نوشتن، اسمبل و اجرای کدهای اسمبلی روی ویندوز x86

### ۲.۱. مراحل کلی توسعه یک برنامه اسمبلی

۱. **نوشتن کد اسمبلی:** با استفاده از ویرایشگر متن یا IDE مناسب ۲. **اسمبل کردن (Assembling):** تبدیل کد اسمبلی به فایل شیء (object file) توسط اسمبلر (مثلاً NASM) ۳. **لینک کردن (Linking):** تبدیل فایل شیء به فایل اجرایی (exe) با استفاده از لینک‌کننده (مانند GoLink، LD یا لینک‌کننده ویژوال استودیو) ۴. **اجرا و دیباگ:** اجرای برنامه و رفع اشکال با دیباگرها

### ۲.۲. مثال عملی: برنامه Hello World با NASM در ویندوز

**کد نمونه (NASM, 32-bit, Win32 API):**

```assembly
section .data
    msg db 'Hello, World!', 0

section .text
    global _main
    extern _printf

_main:
    push msg
    call _printf
    add esp, 4
    ret
```

**اسمبل و لینک:**

```bash
nasm -f win32 hello.asm -o hello.obj
gcc hello.obj -o hello.exe
```

یا با GoLink:

```bash
nasm -f win32 hello.asm -o hello.obj
GoLink.exe /console hello.obj kernel32.dll user32.dll
```

**اجرای برنامه:** hello.exe

**تحلیل:** در این مثال، از تابع printf کتابخانه C استفاده شده است. برای استفاده مستقیم از API ویندوز (مثلاً WriteFile)، باید پارامترها را طبق قرارداد stdcall روی استک قرار دهید و تابع را فراخوانی کنید.

### ۲.۳. مثال عملی: برنامه Hello World با MASM

**کد نمونه (MASM, 32-bit, Win32 API):**

```assembly
.386
.model flat, stdcall
include windows.inc
include kernel32.inc
includelib kernel32.lib

.data
    msg db 'Hello, World!', 0

.code
start:
    push offset msg
    call StdOut
    invoke ExitProcess, 0
end start
```

**اسمبل و لینک:**

```bash
ml /c /coff hello.asm
link /subsystem:console hello.obj
```

**تحلیل:** MASM با Visual Studio یکپارچه است و از دستور invoke برای فراخوانی توابع API استفاده می‌کند که کار با قراردادهای فراخوانی را ساده‌تر می‌کند.

### ۲.۴. مثال عملی: برنامه Hello World با FASM

**کد نمونه (FASM, 32-bit, Win32 API):**

```assembly
format PE console
entry start

include 'win32a.inc'

section '.data' data readable writeable
    msg db 'Hello, World!', 0

section '.code' code readable executable
start:
    invoke  StdOut, msg
    invoke  ExitProcess, 0
```

**اسمبل:**

```bash
fasm hello.asm
```

**تحلیل:** FASM فایل اجرایی را مستقیماً تولید می‌کند و نیازی به لینک‌کننده جداگانه ندارد.

---

## ۳. معرفی و آموزش نصب و راه‌اندازی ابزارهای مورد نیاز

### ۳.۱. اسمبلرها و لینک‌کننده‌ها

|اسمبلر|مزایا|معایب|سیستم‌عامل|
|---|---|---|---|
|NASM|رایگان، مستندات قوی، چندسکویی|سینتکس خاص خود|ویندوز، لینوکس|
|MASM|یکپارچه با ویژوال استودیو، پشتیبانی از API ویندوز|فقط ویندوز، نیاز به نصب ویژوال استودیو|ویندوز|
|FASM|سریع، تولید مستقیم فایل اجرایی|مستندات کمتر نسبت به NASM|ویندوز، لینوکس|
|GoAsm|سبک، مناسب برای ویندوز|جامعه کوچک‌تر|ویندوز|

**تحلیل:** برای شروع، NASM و MASM بهترین گزینه‌ها برای ویندوز x86 هستند. NASM رایگان و مستقل است، MASM برای پروژه‌های ویندوزی و یکپارچه با Visual Studio مناسب است.

### ۳.۲. محیط‌های توسعه (IDE)

|IDE|ویژگی‌ها|پشتیبانی از اسمبلرها|سیستم‌عامل|
|---|---|---|---|
|WinAsm Studio|رایگان، سبک، ویرایشگر پیشرفته، دیباگر داخلی|MASM، FASM (با افزونه)|ویندوز|
|RadASM|قابل تنظیم، پشتیبانی از چند اسمبلر|MASM، NASM، FASM|ویندوز|
|Visual Studio|قدرتمند، دیباگر پیشرفته، یکپارچه با MASM|MASM|ویندوز|
|SASM|کراس‌پلتفرم، پشتیبانی از چند اسمبلر|NASM، MASM، GAS، FASM|ویندوز، لینوکس|
|QEditor (MASM32)|ویرایشگر ساده، همراه با MASM32|MASM|ویندوز|

**تحلیل:** WinAsm Studio و RadASM برای توسعه اسمبلی روی ویندوز بسیار محبوب هستند. Visual Studio برای پروژه‌های بزرگ و دیباگ پیشرفته توصیه می‌شود.

### ۳.۳. دیباگرها و ابزارهای تحلیل

|ابزار|ویژگی‌ها|سیستم‌عامل|کاربرد|
|---|---|---|---|
|x64dbg|رایگان، متن‌باز، رابط کاربری گرافیکی، پشتیبانی از ۳۲ و ۶۴ بیت|ویندوز|دیباگ، مهندسی معکوس|
|OllyDbg|رابط کاربری ساده، پلاگین‌های متعدد|ویندوز|دیباگ، تحلیل باینری|
|WinDbg|ابزار رسمی مایکروسافت، قدرتمند برای دیباگ کرنل و یوزر مود|ویندوز|دیباگ سیستم، کرنل|
|IDA Pro|دیس‌اسمبلر و دیباگر پیشرفته، تحلیل استاتیک و داینامیک|چندسکویی|مهندسی معکوس|
|Ghidra|رایگان، متن‌باز، تحلیل باینری پیشرفته|چندسکویی|مهندسی معکوس|

**تحلیل:** x64dbg و OllyDbg برای دیباگ برنامه‌های اسمبلی روی ویندوز بسیار مناسب هستند. WinDbg برای دیباگ سطح سیستم و کرنل توصیه می‌شود. IDA Pro و Ghidra برای تحلیل پیشرفته و مهندسی معکوس کاربرد دارند.

### ۳.۴. شبیه‌سازها و ابزارهای آموزشی

- **Emu8086:** شبیه‌ساز پردازنده 8086، مناسب برای آموزش مفاهیم پایه
- **Bochs:** شبیه‌ساز کامل x86، مناسب برای تست بوت‌لودر و سیستم‌عامل
- **QEMU:** شبیه‌ساز قدرتمند برای اجرای کدهای سطح پایین و بوت‌لودرها

**تحلیل:** برای یادگیری مفاهیم پایه و تست کدهای سطح پایین، Emu8086 و QEMU بسیار مفید هستند.

### ۳.۵. نصب و راه‌اندازی محیط توسعه (گام‌به‌گام)

#### نصب NASM روی ویندوز

۱. دانلود آخرین نسخه از [سایت رسمی NASM](https://www.nasm.us/) ۲. نصب فایل اجرایی و افزودن مسیر نصب به متغیر PATH ۳. تست نصب با دستور `nasm -v` در CMD

#### نصب MASM و WinAsm Studio

۱. نصب Visual Studio (نسخه Community رایگان کافی است) ۲. نصب MASM (معمولاً همراه با Visual Studio نصب می‌شود) ۳. دانلود و نصب WinAsm Studio از [winasm.org](https://winasm.org/) ۴. تنظیم مسیر اسمبلر و لینک‌کننده در تنظیمات IDE

#### نصب x64dbg

۱. دانلود نسخه مناسب از [صفحه رسمی GitHub](https://github.com/x64-dbg) ۲. استخراج فایل‌ها و اجرای x64dbg.exe یا x32dbg.exe

#### نصب Emu8086

۱. دانلود از [سایت رسمی](https://emu8086-microprocessor-emulator.software.informer.com/) ۲. نصب و اجرای برنامه

---

## ۴. آموزش نحوه کامپایل و اجرای کدهای اسمبلی با ابزارهای مختلف

### ۴.۱. کامپایل و اجرای کد با NASM و GCC/GoLink

**مراحل (۳۲ بیت):** ۱. نوشتن کد در فایل hello.asm ۲. اسمبل: `nasm -f win32 hello.asm -o hello.obj` ۳. لینک با GCC: `gcc hello.obj -o hello.exe` یا با GoLink: `GoLink.exe /console hello.obj kernel32.dll user32.dll` ۴. اجرا: `hello.exe`

**مراحل (۶۴ بیت):** ۱. اسمبل: `nasm -f win64 hello.asm -o hello.obj` ۲. لینک با GCC: `gcc hello.obj -o hello.exe` یا با لینک‌کننده ویژوال استودیو

### ۴.۲. کامپایل و اجرای کد با MASM

۱. اسمبل: `ml /c /coff hello.asm` ۲. لینک: `link /subsystem:console hello.obj` ۳. اجرا: `hello.exe`

### ۴.۳. کامپایل و اجرای کد با FASM

۱. اسمبل و تولید فایل اجرایی: `fasm hello.asm` ۲. اجرا: `hello.exe`

### ۴.۴. نکات مهم در کامپایل و لینک

- در ویندوز ۶۴ بیت، باید shadow space (۳۲ بایت) را قبل از فراخوانی توابع API رزرو کنید.
- برای استفاده از توابع کتابخانه C (مانند printf)، باید فایل شیء را با libc لینک کنید.
- برای تولید فایل اجرایی مستقل و کوچک، مستقیماً از API ویندوز استفاده کنید و از لینک با libc خودداری کنید.

---

## ۵. تمرین‌های روزانه و پروژه‌های کوچک برای یادگیری خودآموز و مستمر

### ۵.۱. تمرین‌های روزانه (سطح‌بندی شده)

#### هفته اول: مفاهیم پایه و دستورات ساده

- نوشتن برنامه‌ای برای جمع دو عدد و نمایش نتیجه
- پیاده‌سازی حلقه for و while در اسمبلی
- کار با رجیسترها: انتقال داده بین رجیسترها و حافظه
- استفاده از دستور CMP و پرش‌های شرطی (JE, JNE, JG, JL)

#### هفته دوم: کار با استک و توابع

- پیاده‌سازی تابع جمع دو عدد با استفاده از استک
- استفاده از PUSH و POP برای ذخیره و بازیابی داده‌ها
- پیاده‌سازی فراخوانی و بازگشت از تابع (CALL/RET)
- تمرین مدیریت پارامترها و مقدار بازگشتی

#### هفته سوم: کار با حافظه و آرایه‌ها

- تعریف آرایه در بخش داده و پیمایش آن با حلقه
- جمع عناصر آرایه و نمایش نتیجه
- پیاده‌سازی جستجوی خطی در آرایه

#### هفته چهارم: کار با رشته‌ها و ورودی/خروجی

- نمایش رشته روی کنسول با استفاده از WriteFile یا printf
- خواندن ورودی از کاربر و پردازش آن
- معکوس کردن یک رشته

### ۵.۲. پروژه‌های کوچک هفتگی

- **ماشین حساب ساده:** جمع، تفریق، ضرب و تقسیم دو عدد
- **مرتب‌سازی آرایه:** پیاده‌سازی الگوریتم Bubble Sort یا Selection Sort
- **تولید جدول ضرب:** نمایش جدول ضرب ۱ تا ۱۰
- **تبدیل عدد دسیمال به باینری و بالعکس**

### ۵.۳. پروژه‌های میان‌مدت و نهایی

- **پیاده‌سازی بوت‌لودر ساده:** نمایش پیام روی صفحه هنگام بوت (با QEMU یا Emu8086)
- **نوشتن درایور ساده برای دستگاه مجازی**
- **تحلیل و مهندسی معکوس یک فایل اجرایی ساده با x64dbg**
- **پیاده‌سازی مدیریت حافظه ساده (مانند malloc/free) در اسمبلی**
- **ساخت ابزار کوچک برای خواندن و نوشتن فایل‌ها با API ویندوز**

### ۵.۴. تمرین‌های پیشرفته

- **بهینه‌سازی کد با استفاده از دستورالعمل‌های SIMD (SSE, AVX)**
- **نوشتن کد اسمبلی اینلاین در C/C++ و بررسی تفاوت عملکرد**
- **تحلیل باینری و استخراج الگوریتم از فایل اجرایی با Ghidra یا IDA Pro**
- **نوشتن روتین وقفه و مدیریت وقفه‌های سخت‌افزاری**

---

## ۶. مفاهیم مرتبط با برنامه‌نویسی سیستم: مدیریت حافظه، وقفه‌ها، رجیسترها، تعامل با سخت‌افزار

### ۶.۱. مدیریت حافظه، استک، هیپ و مدل‌های آدرس‌دهی

**استک (Stack):** ساختار LIFO برای ذخیره پارامترها، آدرس بازگشت و متغیرهای محلی. رجیستر ESP (یا RSP در ۶۴ بیت) اشاره‌گر استک است. رجیستر EBP (یا RBP) برای مدیریت فریم استک هر تابع استفاده می‌شود.

**هیپ (Heap):** حافظه پویا که با توابعی مانند malloc/calloc تخصیص داده می‌شود. مدیریت آن در اسمبلی نیازمند پیاده‌سازی الگوریتم‌های تخصیص و آزادسازی است.

**مدل‌های آدرس‌دهی:**

- **Immediate:** مقدار ثابت در دستور
- **Direct:** آدرس حافظه مشخص
- **Indirect:** آدرس از طریق رجیستر
- **Indexed:** ترکیب پایه و شاخص
- **Base + Index + Displacement:** ترکیب پیچیده‌تر برای دسترسی به آرایه‌ها و ساختارها

### ۶.۲. وقفه‌ها، روتین‌های وقفه و تعامل با سخت‌افزار

**وقفه (Interrupt):** سیگنالی از سخت‌افزار یا نرم‌افزار که باعث توقف اجرای فعلی و اجرای روتین وقفه (ISR) می‌شود. در ویندوز، وقفه‌ها بیشتر در سطح کرنل و درایورها مدیریت می‌شوند، اما در سطح یوزر مود نیز می‌توان با برخی وقفه‌ها (مانند INT 3 برای دیباگ) کار کرد.

**ساختار IDT (Interrupt Descriptor Table):** جدولی که آدرس روتین‌های وقفه را نگهداری می‌کند. هر ورودی شامل آدرس ISR، سگمنت کد و ویژگی‌های وقفه است.

**نمونه کد روتین وقفه (در سطح پایین):**

```assembly
isr_keyboard:
    ; خواندن کد اسکن از پورت 0x60
    in al, 0x60
    ; پردازش کد اسکن
    ; ارسال EOI به کنترلر وقفه
    mov al, 0x20
    out 0x20, al
    iret
```

**تحلیل:** در برنامه‌نویسی سیستم و توسعه درایور، درک عمیق از وقفه‌ها و مدیریت آن‌ها ضروری است.

### ۶.۳. رجیسترها و نقش آن‌ها در برنامه‌نویسی سیستم

- **EAX/RAX:** رجیستر اصلی برای عملیات ریاضی و مقدار بازگشتی توابع
- **EBX/RBX:** رجیستر پایه برای آدرس‌دهی حافظه
- **ECX/RCX:** شمارنده حلقه‌ها و پارامتر اول در x64
- **EDX/RDX:** پارامتر دوم در x64، عملیات تقسیم و ضرب
- **ESI/RSI, EDI/RDI:** شاخص‌های منبع و مقصد برای عملیات رشته‌ای
- **ESP/RSP:** اشاره‌گر استک
- **EBP/RBP:** اشاره‌گر پایه فریم استک
- **EIP/RIP:** اشاره‌گر دستورالعمل بعدی

**تحلیل:** در برنامه‌نویسی سیستم، مدیریت صحیح رجیسترها و آگاهی از نقش هرکدام برای جلوگیری از خطا و بهینه‌سازی عملکرد حیاتی است.

### ۶.۴. تعامل اسمبلی با زبان‌های سطح بالا و قراردادهای فراخوانی

**قراردادهای فراخوانی (Calling Conventions):**

- **cdecl:** پارامترها روی استک (راست به چپ)، پاکسازی استک با فراخواننده، مقدار بازگشتی در EAX
- **stdcall:** پارامترها روی استک (راست به چپ)، پاکسازی استک با تابع، مقدار بازگشتی در EAX (استاندارد WinAPI)
- **fastcall:** پارامترهای اول در رجیسترها (ECX, EDX)، بقیه روی استک
- **thiscall:** برای توابع عضو کلاس‌های C++، اشاره‌گر this در ECX
- **Microsoft x64:** چهار پارامتر اول در RCX, RDX, R8, R9، بقیه روی استک، shadow space الزامی

**نمونه کد فراخوانی تابع C از اسمبلی:**

```assembly
extern printf
section .data
    msg db 'Value: %d', 10, 0
section .text
    global main
main:
    push 10
    push msg
    call printf
    add esp, 8
    ret
```

**تحلیل:** برای تعامل با C/C++ باید قرارداد فراخوانی و نحوه ارسال پارامترها را رعایت کنید. در x64، پارامترها در رجیسترها قرار می‌گیرند و باید ۳۲ بایت shadow space رزرو شود.

---

## ۷. امنیت، مهندسی معکوس و تحلیل باینری با استفاده از اسمبلی

### ۷.۱. مهندسی معکوس و تحلیل باینری

- **دیس‌اسمبلرها:** IDA Pro، Ghidra، x64dbg، OllyDbg
- **تحلیل استاتیک:** بررسی ساختار فایل اجرایی، استخراج توابع، تحلیل جریان داده
- **تحلیل داینامیک:** اجرای برنامه در دیباگر، بررسی رفتار، یافتن آسیب‌پذیری‌ها

**نمونه کاربردها:**

- کرک نرم‌افزارها و بای‌پس کردن محدودیت‌ها
- تحلیل بدافزار و استخراج الگوریتم‌ها
- بررسی امنیتی و کشف آسیب‌پذیری‌ها

### ۷.۲. امنیت و بهینه‌سازی کد اسمبلی

- **محافظت از استک (Stack Protection):** استفاده از canary و بررسی سرریز استک
- **بهینه‌سازی با SIMD (SSE, AVX):** استفاده از دستورالعمل‌های برداری برای افزایش سرعت پردازش داده‌ها (مثلاً در پردازش تصویر یا رمزنگاری).
- **کاهش حملات ROP/JOP:** محدود کردن استفاده از دستورالعمل‌های خطرناک و بررسی جریان کنترل

---

## ۸. منابع یادگیری (کتاب، ویدیو، وب‌سایت) برای مطالعه‌ی بیشتر

### ۸.۱. کتاب‌های پیشنهادی (فارسی و انگلیسی)

|عنوان|نویسنده|توضیحات|
|---|---|---|
|Assembly Language for x86 Processors|Kip R. Irvine|مرجع جامع برای آموزش اسمبلی x86، مثال‌های متعدد، تمرین‌های عملی|
|Programming from the Ground Up|Jonathan Bartlett|آموزش اسمبلی با رویکرد مفهومی و پروژه‌محور|
|The Art of Assembly Language|Randall Hyde|مرجع پیشرفته، پوشش مفاهیم عمیق و بهینه‌سازی|
|Modern X86 Assembly Language Programming|Daniel Kusswurm|آموزش ۳۲ و ۶۴ بیت، SSE و AVX، بهینه‌سازی|
|X86 Assembly Language and C Fundamentals|Joseph Cavanagh|آموزش ترکیبی اسمبلی و C، مثال‌های کاربردی|
|Reverse Engineering for Beginners|Dennis Yurichev|آموزش مهندسی معکوس و تحلیل باینری با تمرکز بر اسمبلی|
|جزوه و ویدیوهای فارسی|sariasan.com، maktabkhooneh.org|آموزش صفر تا صد اسمبلی، مثال‌های عملی، پروژه‌های کوچک|

### ۸.۲. دوره‌ها و ویدیوهای آموزشی

- **مکتب‌خونه:** دوره تسلط بر برنامه‌نویسی اسمبلی x86 (ویدیو و تمرین عملی)
- **یوتیوب:** کانال‌هایی مانند "Low Level Learning"، "Jacob Sorber"، "Assembly Programming"
- **Udemy:** دوره‌های "x86 Assembly Language Programming" و "Reverse Engineering with x64dbg"

### ۸.۳. وب‌سایت‌ها و منابع آنلاین

- [NASM Documentation](https://www.nasm.us/)
- [WinAsm Studio](https://winasm.org/)
- [x64dbg](https://github.com/x64-dbg)
- [GeeksforGeeks: x86 vs x64](https://www.geeksforgeeks.org/computer-organization-architecture/difference-between-x64-and-x86-architecture/)
- [Exercism x86-64 Assembly Track](https://exercism.org/tracks/x86-64-assembly) (تمرین‌های مرحله‌ای)
- [GitHub: پروژه‌های نمونه NASM](https://github.com/albertopetrache/nasm-win64-projects)
- [Coddy Reference: IDEهای محبوب اسمبلی](https://ref.coddy.tech/assembly/popular-assembly-ides)
- [LinuxLinks: کتاب‌های رایگان آموزش اسمبلی](https://www.linuxlinks.com/excellent-free-books-learn-assembly/)
- [MoldStud: فهرست ۵۰ کتاب و منبع برتر اسمبلی](https://moldstud.com/articles/p-discover-50-essential-books-and-ebooks-for-assembly-language-enthusiasts-to-elevate-your-skills-and-master-the-art-of-programming)

### ۸.۴. ابزارهای تمرین و پروژه

- [Emu8086](https://emu8086-microprocessor-emulator.software.informer.com/)
- [QEMU](https://www.qemu.org/)
- [Bochs](https://bochs.sourceforge.io/)
- [IDA Pro](https://hex-rays.com/ida-pro/)
- [Ghidra](https://ghidra-sre.org/)

---

## ۹. ساختاردهی مطالب به‌صورت مرحله‌ای و منظم برای یادگیری روزانه

### ۹.۱. برنامه‌ی پیشنهادی یادگیری روزانه (۸ هفته)

|هفته|موضوعات اصلی|تمرین‌ها و پروژه‌ها|
|---|---|---|
|۱|مبانی معماری x86، رجیسترها، حافظه، استک|جمع و تفریق ساده، حلقه‌ها، کار با رجیسترها|
|۲|دستورات پایه، آدرس‌دهی، کار با داده‌ها|پیاده‌سازی حلقه‌ها، مقایسه و پرش شرطی|
|۳|کار با استک و توابع، مدیریت پارامترها|پیاده‌سازی توابع بازگشتی، مدیریت استک|
|۴|کار با آرایه‌ها و رشته‌ها، ورودی/خروجی|مرتب‌سازی آرایه، معکوس کردن رشته|
|۵|کار با فایل‌ها و API ویندوز|خواندن و نوشتن فایل، نمایش پیام با MessageBox|
|۶|مدیریت حافظه، استک و هیپ|پیاده‌سازی malloc/free ساده|
|۷|وقفه‌ها، روتین‌های وقفه، تعامل با سخت‌افزار|نوشتن ISR ساده، کار با پورت‌های I/O|
|۸|مهندسی معکوس، امنیت، بهینه‌سازی|تحلیل باینری، استفاده از SSE/AVX، پروژه نهایی|

### ۹.۲. نکات کلیدی برای یادگیری مؤثر

- **تمرین مستمر:** هر روز حداقل یک تمرین یا پروژه کوچک انجام دهید.
- **مطالعه منابع متنوع:** از کتاب، ویدیو، وب‌سایت و پروژه‌های عملی استفاده کنید.
- **یادداشت‌برداری و مستندسازی:** نکات مهم و خطاهای رایج را یادداشت کنید.
- **استفاده از دیباگر:** کدهای خود را با x64dbg یا OllyDbg دیباگ کنید تا درک عمیق‌تری از اجرای برنامه پیدا کنید.
- **پرسش و پاسخ:** در انجمن‌ها و گروه‌های تخصصی سوالات خود را مطرح کنید و از تجربیات دیگران بهره ببرید.

---

## جمع‌بندی و توصیه‌های نهایی

یادگیری زبان اسمبلی، به‌ویژه روی پلتفرم x86 و سیستم‌عامل ویندوز، نیازمند صبر، تمرین مستمر و مطالعه منابع متنوع است. این زبان نه‌تنها درک عمیق‌تری از عملکرد سخت‌افزار و سیستم‌عامل به شما می‌دهد، بلکه مهارت‌های شما را در بهینه‌سازی، امنیت، مهندسی معکوس و توسعه سیستم ارتقا می‌دهد. با پیروی از برنامه‌ی مرحله‌ای ارائه‌شده، استفاده از ابزارهای مناسب، انجام تمرین‌های روزانه و پروژه‌های عملی، و مطالعه منابع پیشنهادی، می‌توانید به سطح تخصصی در زبان اسمبلی دست یابید.

**نکته کلیدی:** تمرین، دیباگ و تحلیل کدهای نمونه، و تعامل با جامعه برنامه‌نویسان اسمبلی، مسیر یادگیری شما را هموارتر و پربارتر خواهد کرد. موفق باشید! عالیه امیرحسین! همین که با این دقت و انگیزه برنامه‌ریزی کردی، نشون می‌ده که مسیر درستی رو انتخاب کردی. من همین حالا دارم یک برنامه‌ی جامع برای یادگیری زبان اسمبلی روی پلتفرم x86 و سیستم‌عامل ویندوز آماده می‌کنم که شامل آموزش تئوری، تمرین عملی، معرفی ابزارها، نحوه‌ی نصب و اجرای کدها، و مسیر یادگیری روزانه‌ست.

🧠 موضوعاتی که قراره پوشش بدم شامل:

- مفاهیم پایه و پیشرفته اسمبلی (از رجیسترها تا مدیریت وقفه‌ها)
- ابزارهایی مثل NASM، MASM، Debuggerها، و شبیه‌سازها
- آموزش نصب و راه‌اندازی محیط توسعه روی ویندوز
- نحوه‌ی اسمبل و اجرای کدهای اسمبلی
- تمرین‌های روزانه، پروژه‌های کوچک، و مسیر یادگیری مرحله‌ای
- کاربردهای اسمبلی در برنامه‌نویسی سیستم، مهندسی معکوس، و بهینه‌سازی سطح پایین

⏳ این کار چند دقیقه زمان می‌بره، پس اگر خواستی می‌تونی فعلاً بری سراغ کار دیگه — من اینجا هستم و گزارش کامل رو توی همین گفتگو ذخیره می‌کنم.